{
  "name": "spattoo-web",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "// lockfile": [
    "NEVER regenerate package-lock.json from scratch (rm package-lock.json, or rm -rf node_modules",
    "followed by npm install) on macOS. npm then records ONLY the host platform's optional binaries:",
    "sharp drops from 24 platform packages to 2 (darwin only), the linux ones vanish, and the Vercel",
    "build \u2014 which runs npm ci on linux-x64 \u2014 fails. Verified: it broke a deploy on 2026-07-24.",
    "To change a dependency, edit package.json and run plain `npm install` so the existing lock is",
    "updated in place. Sanity check before pushing a lockfile change:",
    "  grep -c '\"node_modules/@img/sharp-' package-lock.json   # expect 24, not 2",
    "  grep -c '\"node_modules/@next/swc-' package-lock.json     # expect 8"
  ],
  "// security": [
    "npm audit reports 3 remaining highs, all inside next@16.2.11's own dependency tree:",
    "postcss 8.4.31 (exact pin) and sharp 0.34.5 (optional). 16.2.11 IS npm's `latest` \u2014 there",
    "is no stable Next that ships patched versions yet, `npm audit fix --force` would DOWNGRADE",
    "next to 9.3.3, and npm `overrides` are silently ignored for both (verified on a clean tree).",
    "Neither is reachable here: postcss runs at build time over our own CSS, and next/image is",
    "used ONLY for two local logo files with no images.remotePatterns configured, so sharp never",
    "processes attacker-supplied bytes. TRIPWIRE: adding images.remotePatterns \u2014 i.e. letting",
    "next/image optimize baker/customer uploads \u2014 makes the sharp libvips CVEs reachable and this",
    "must be re-assessed. See SEC-WEB-2 in spattoo-docs/security/spattoo-web-SECURITY_ACTION_PLAN.md."
  ],
  "scripts": {
    "prepare": "git config core.hooksPath .githooks 2>/dev/null || true",
    "check:feature-docs": "node ../spattoo-docs/bin/check-feature-docs.mjs --repo spattoo-web",
    "check:plan-copy": "node ../spattoo-docs/bin/check-plan-copy.mjs",
    "dev:marketing": "npm run dev --workspace marketing",
    "dev:app": "npm run dev --workspace app",
    "build:marketing": "npm run build --workspace marketing",
    "build:app": "npm run build --workspace app",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  }
}
